// const holidaySales = (
//   product: string,
//   sales: number,
//   tax: number,
//   discount: number
// ): number => {
//   // let dept: string = "Tech";

//   let price: number = 0;

//   if (product === "pens") price = 29;
//   if (product === "erasers") price = 19;
//   if (product === "pencils") price = 59;

//   let totalSalesForIndividualProduct: number = price * sales;

//   return (
//     totalSalesForIndividualProduct -
//     tax * totalSalesForIndividualProduct -
//     discount
//   );
// };

// const pens: number = holidaySales("pens", 20, 0.15, 0.25);
// const erasers: number = holidaySales("erasers", 15, 0.25, 0.2);
// const pencils: number = holidaySales("pencils", 18, 0.1, 0.45);
// console.log(pens, erasers, pencils);

// function multiplication(num1: number, num2: number): number {
//   return num1 * num2;
// }

// console.log(multiplication(1, 5));
// console.log(multiplication(2, 12));
// console.log(multiplication(100, 5));

// function emailList(email: string): string {
//   return email;
// }

// console.log(emailList(15))

// let add = (num: number, bool: boolean): number => num + bool

// let pluralize = function (word: string): string {
//   return word + "s";
// };

// console.log(pluralize("cake"));

// function decideOperation(num: number, bool: boolean): number {
//   return bool ? num + 1 : num - 1;
// }

// console.log(decideOperation(10, true));
// console.log(decideOperation(10, false));

// function myArr(arr: number[]): number[] {
//   return arr;
// }
